<!DOCTYPE html>
<html lang="ja">
  <head>
    <h2>メモアプリ</h2>
  </head>

  <body>
    <div>
      <p><%= @title %></p>
      <p><%= @content %></p>

    <div class="btn btn-edit">
      <a href="/memos/<%= params[:id] %>/edit">変更</a>
    </div>

    <form action="/memos/<%= params[:id] %>" method="post">
      <div class="btn btn-delete">
        <input type="hidden" name="_method" value="delete">
        <input type="submit" value="削除">
      </div>
    </form>
  </body>
</html>
